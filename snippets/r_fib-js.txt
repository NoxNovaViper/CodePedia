// 1. Input
const n = 6;

// 2. Recursive Function
function fibonacci(n) {
    if (n <= 1) return n;
    return fibonacci(n - 1) + fibonacci(n - 2);
}

// 3. Execution
console.log(`Fibonacci at position ${n} is ${fibonacci(n)}`);