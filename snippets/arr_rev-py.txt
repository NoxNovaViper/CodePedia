# 1. Define the Array
arr = [10, 20, 30, 40, 50]

# 2. Reverse (Manual Swap)
n = len(arr)
for i in range(n // 2):
    # Swap elements using a temporary variable
    temp = arr[i]
    arr[i] = arr[n - 1 - i]
    arr[n - 1 - i] = temp

print("Reversed Array:", arr)