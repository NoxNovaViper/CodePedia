// 1. Define the Array
int arr[] = {15, 72, 8, 91, 23, 56};
int n = sizeof(arr)/sizeof(arr[0]);

// 2. Find Max
int maxVal = arr[0];
for(int i = 1; i < n; i++) {
    if(arr[i] > maxVal) {
        maxVal = arr[i];
    }
}
cout << "Maximum Value: " << maxVal << endl;