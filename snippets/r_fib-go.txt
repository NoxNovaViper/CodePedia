package main
import "fmt"

// Recursive Function
func fibonacci(n int) int {
    if n <= 1 {
        return n
    }
    return fibonacci(n-1) + fibonacci(n-2)
}

func main() {
    n := 6
    fmt.Printf("Fibonacci at position %d is %d\n", n, fibonacci(n))
}