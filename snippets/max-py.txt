# 1. Define the Array
arr = [15, 72, 8, 91, 23, 56]

# 2. Find Max (Manual Loop)
max_val = arr[0]  # Start by assuming the first is the biggest
for i in range(1, len(arr)):
    if arr[i] > max_val:
        max_val = arr[i]

print("Maximum Value:", max_val)