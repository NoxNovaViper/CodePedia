# 1. Define the Array
arr = [64, 34, 25, 12, 22, 11, 90]

# 2. Find Min (Manual Loop)
min_val = arr[0]  # Assume the first element is the smallest
for i in range(1, len(arr)):
    if arr[i] < min_val:
        min_val = arr[i]

print("Minimum Value:", min_val)