    int n;
    cin<<n;
    bool is_prime = n>1;
    for (int i = 2; i * i <= n; i++) {
        if (n % i == 0){
            is_prime = false;
            break;}}
    if (is_prime)
        cout<<"Prime";
    else    
        cout<<"Not Prime";