# 1. Define the Array
arr = [10, 20, 30, 40, 50]
n = 5
pos = 2  # We want to delete '30' (index 2)

# 2. Shift elements to the left
for i in range(pos, n - 1):
    arr[i] = arr[i + 1]

# 3. Shrink the visual size
arr.pop() # Or just ignore the last element

print("After Deletion:", arr)