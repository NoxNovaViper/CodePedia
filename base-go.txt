matrix := [][]int{
    {1, 2, 3},
    {4, 5, 6},
    {7, 8, 9},
}

rows := len(matrix)
cols := len(matrix[0])

for r := 0; r < rows; r++ {
    for c := 0; c < cols; c++ {
 
        fmt.Println(matrix[r][c])
    }
}